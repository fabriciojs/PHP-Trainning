<?php
	// superglobals
	// super globais são variáveis disponíveis 
	// em todo e qualquer escopo.

	// $_SERVER       informações sobre o servidor e a requisição
	// $_POST         parâmetros da requisição enviados pelo método
	// 				  POST (no corpo da requisição)
	// $_GET          parâmetros da requisição enviados pelo método
	//                GET (query string)
	// $_REQUEST      a fusão dos dois arrays $_POST e $_GET (todo input)
	// $_FILES        arquivos enviados por upload na requisição
	// $_ENV          variáveis de ambiente no escopo do script
	// $_COOKIE       os cookies enviados pelo cliente
	// $_SESSION      as variáveis guardadas na sessão
